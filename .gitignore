# Кэши и артефакты сборки
/build/
/Build/
/_build/
/.cache/
# Было `**.*o` — этот шаблон прятал и po/ru.po с po/en.po, то есть переводы
# не попадали в репозиторий.
*.o

# Сгенерированные файлы ресурсов
gresources.c
gresources.h
compile_commands.json
.gdb_history

# Скомпилированные бинарники в корне
way-shell
way-sh

# Скомпилированная gschema — собирается meson-ом в build/data/
data/gschemas.compiled

# Шаблон переводов — генерируется целью way-shell-pot из POTFILES.in.
# Сами po/*.po отслеживаются.
po/*.pot

# Личное
repomix-output.xml
RoadMap.md
.vscode
